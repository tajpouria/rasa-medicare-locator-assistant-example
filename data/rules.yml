version: "3.0"

rules:
  - rule: Activate Facility Form
    steps:
      - intent: inform
      - action: facility_form
      - active_loop: facility_form

  - rule: Submit Facility Form
    condition:
      - active_loop: facility_form
    steps:
      - action: facility_form
      - active_loop: null
      - slot_was_set:
        - requested_slot: null
      - action: action_submit_facility_form
